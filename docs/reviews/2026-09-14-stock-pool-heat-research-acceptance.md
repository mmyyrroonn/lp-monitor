# 股票池完整目录与热度研究验收

日期：2026-09-14
分支：`codex/stock-pool-heat-research`
范围：H1–H4 代码实现与离线验收；H5 实链实测未运行。

## 结论

H1–H4 已实现并通过离线回归。目录、历史作业、原生回放导出和固定时间滚动研究均保留缺口、未知、未计价和不完整状态，不把它们填成零，也不改变实时阈值、通知或交易边界。

H5 尚未授权运行，因此目前没有真实链上目录完整性、历史完整性、研究样本充分性或候选规则有效性的结论，也没有 LP 净收益证明。

## 代码与离线验证

| 项目          | 结果                                                                                  |
| ------------- | ------------------------------------------------------------------------------------- |
| H1 目录与恢复 | 完成；支持固定目标区块、版本化快照、失败分类和续跑；follow 仍为 latest-only           |
| H2 存储与审计 | 完成；内容寻址压缩、旧格式读取、离线 compact/audit 和字节拆分                         |
| H3 历史作业   | 完成；固定 spec/input snapshot、独立 study DB、预算/截止时间、持久状态和恢复          |
| H4.1 回放导出 | 完成；v2 gzip segments、未压缩逻辑哈希、压缩哈希、路径/篡改检查、v1 兼容              |
| H4.2 滚动研究 | 完成；`(T-duration,T]`、chain-time/recorded-observed、固定 train/validation/test 分段 |
| TypeScript    | `pnpm typecheck` 通过                                                                 |
| 测试          | 106 个测试文件，869 个测试通过                                                        |
| Lint          | `pnpm lint` 通过                                                                      |
| Build         | `pnpm build` 通过                                                                     |
| Diff 检查     | `git diff --check` 通过                                                               |

定向回归包括 H3 13 个测试、H4.1 39 个测试、H4.2 5 个测试、P6 评估 21 个测试和告警恢复 5 个测试。Uniswap 依赖产生的 sourcemap warning 不影响退出码。

## 存储效果

离线基准写入 10,000 个事件；compact 与可比较的原始存储基线相比，体积比例为 `0.5093943914931628`，约减少 50.94%。基准结果和口径见 `docs/reviews/2026-09-13-heat-storage-baseline.md`。该结果是本地合成负载效果，不外推为真实链上日均资源。

## 未运行项目

以下项目需要用户在新的执行窗口明确目标范围、固定区块/时间边界和预算后才能运行：

| 验收字段             | 当前状态       |
| -------------------- | -------------- |
| `directoryComplete`  | 未运行，未测量 |
| `historyComplete`    | 未运行，未测量 |
| `studySufficient`    | 未运行         |
| `candidateValidated` | 未运行         |

未执行真实 RPC 历史采集、第三方数据购买、常驻服务、钱包/交易、真实通知和线上阈值修改。H5 实测结果必须另行记录实际 RPC、覆盖、原始 bytes、DB/WAL 增量、耗时、失败/重试和缺口，不能用本报告的离线数字代替。
