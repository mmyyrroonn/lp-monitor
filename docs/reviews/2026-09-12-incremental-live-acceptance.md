# 实时增量与独立历史回顾验收（2026-09-12）

## 结果

修正基于 main 的 3507699，在 codex/incremental-live-metrics 实现。普通实时批次不再重建全部协议投影、重新估值全部历史或扫描全部未知时间日志；增量状态和分钟贡献持久化，滑动窗口外的衍生缓存过期，原始证据保留。首次升级/投影版本变化、显式离线 project/replay 仍可重建。

history 命令接受包含两端的区块范围，使用独立数据库快照，本地完整覆盖优先，仅补缺失的 discovery/operation 区间；不修改源实时数据库或触发通知。补齐后的 review.sqlite 可作为下一次输入复用，详见[历史回顾说明](../history-review.md)。

## 验证证据

- pnpm test：79 个文件、774 个测试全部通过，25.51 秒。日志（换行规范化为 LF）：[incremental-tests.log](../../artifacts/incremental-tests.log)。依赖包原有 source-map 缺失提示仍存在，不影响测试结果。
- pnpm typecheck、pnpm lint、pnpm build：均退出 0。
- git diff --check：通过。
- pnpm lp history --help：退出 0，无 RPC。
- 398 笔历史交易的合成样本随后追加一笔：新增解码 1 次、估值 1 次、全量重建 0 次；raw 共 399 笔仍保留，实时事件读取少于 190 笔。
- 覆盖重复批次、移除/retime、窗口过期、历史覆盖修订撤回、事务回滚、关闭重开数据库、只读缓存读取、报价前置上下文、历史缺口/注册表变更及源库隔离。
- 实际 SQL 的 EXPLAIN QUERY PLAN 回归验证 raw 高度索引与 accepted 区间候选索引驱动；缺少近期边界时不回退全历史。旧只读数据库缺新索引的错误先复现，再验证兼容查询且不自动创建索引。

## 独立审查

审查发现并修复了缺边界全历史回退、过旧边界放大未知记录窗口、有界结果但底层全历史扫描、旧只读库新索引兼容性四类问题。最终复核无剩余 Critical/Important 阻塞。

本次未启动真实 RPC 采集、长时观察或两小时测试。离线结构性回归不代表实链延迟验收；旧 P6 长时验收状态保持原记录。
