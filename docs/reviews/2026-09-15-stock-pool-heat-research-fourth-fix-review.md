# 股票池与热度研究：第四轮修复复核

审查日期：2026-09-15。基线 `d90206e`，目标 `main@dcca9c0`，范围 `d90206e..dcca9c0`。

**结论：本次修复复核通过。上一轮 J01–J03 全部关闭；本次改动及相邻路径未发现新的可操作缺陷。** 结论来自补丁审查、独立原始反例重跑及全量离线测试。

本轮仅新增此报告，未修改业务代码、Git 提交或生产数据库。复现使用模拟事件、临时配置和临时数据库，没有连接真实 RPC 或启动采集。

## J01–J03 关闭证据

| 问题                         | 结果 | 独立复核                                                                                                                                                                                                                                                |
| ---------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| J01 窗外分钟事件污染已有窗口 | 关闭 | 在精确 5200 秒 1,000 USD 基线成交、6110 秒 5,000 USD 前向成交之外，加入 minuteStartSec=18000 的事件，15/60/180 分钟结果与加入前完全一致（对全部返回字段做深度相等断言）。前向窗内再加入 6600 分钟 2,000 USD，前向合计为 7,000 USD，基线仍为 1,000 USD。 |
| J02 基线随反应延迟偏移       | 关闭 | 触发 6050 秒，5600/6070/6500 秒分别成交 1,000/9,000/5,000 USD；delay=1 和 delay=5 均返回前向 5,000 USD、触发前基线 1,000 USD、倍率 5。等待期成交未进入基线。                                                                                            |
| J03 默认预算未扣预检         | 关闭 | 临时配置 recorderMaxRpcCalls=1，省略显式参数，只创建一个 reader，实际与记录均为 1 次，然后 paused。显式预算路径仍正确；先用 1 次暂停，再续跑 22 次完成，累计 23 次。正常运行的两个 reader 使用同一个绝对 deadline。                                     |

关键代码：`src/replay/study-run.ts` 先排除完全位于窗口左右两侧的分钟事件，再处理相交边界；基线使用实际 `triggerSec`。`src/ops/history-job.ts` 先归一化显式或配置预算，再扣除目标预检消耗。

补充旧边界检查全部通过：

- 触发前的 6020 秒成交不会进入 6050 秒触发的后续结果窗。
- 与左/右边界相交的分钟精度事件仍保持 incomplete。
- 缺少最后相交分钟的覆盖仍保持 incomplete，金额不填零。
- 6920 秒的最后一分钟成交得到 activeMinutes=1、longestActiveRunMinutes=1。
- 上轮零后续成交的原始数据及配置通过 `studyWithConfig` 重跑，仍输出 insufficient-evidence，未重新误报 candidate-supported。

## 验证记录

| 检查                              | 结果                                                            |
| --------------------------------- | --------------------------------------------------------------- |
| pnpm build                        | 通过；先构建，确保依赖 dist 的测试使用本次代码                  |
| pnpm typecheck                    | 通过                                                            |
| pnpm test                         | 112 个文件、911 个测试全部通过；本轮全量一次通过，用时 50.92 秒 |
| pnpm lint                         | 通过                                                            |
| git diff --check d90206e..dcca9c0 | 通过                                                            |
| 独立窗口、基线、预算及恢复反例    | 全部通过                                                        |

复现证据：

- 窗口、基线与旧反例：`C:/Users/myron/AppData/Local/Temp/review-dcca9c0-wYjtO5/window-and-baseline-evidence.json`；同目录保留零后续成交研究输出。
- 默认预算与暂停恢复：`C:/Users/myron/AppData/Local/Temp/h3-limits-default-KgZ8tC/results.json`。
- 预算复现脚本：`C:/Users/myron/AppData/Local/Temp/h3-limits-default-review-20260914.mts`。
- 全量测试日志：`C:/Users/myron/AppData/Local/Temp/lp-review-dcca9c0-tests.log`。

## 结论范围

本次通过的是上述修复的代码与离线行为复核，没有重新审计所有未修改模块，也没有进行真实 RPC、长期采集、生产规模容量或热度阈值有效性验收。H5 不在本轮范围内。
