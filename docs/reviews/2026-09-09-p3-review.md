# P3 final correctness review — 2026-09-09

**PASS — no outstanding material correctness findings in the reviewed P3 scope.**

Reviewed the uncommitted P3 implementation against Tasks 3.1–3.3 and design section 9, including the three earlier review reports. This is a code/replay review; historical acceptance artifacts and the broader suite are owned by the parent task.

## Finding resolved during this review

The RWA current-minute aggregate previously bypassed current-prefix coverage and could emit zero or a partial sum while pool windows correctly reported a gap. `src/storage/metric-store.ts` now gates this aggregate on a valid accepted prefix, exposes `available`, and returns `activity: null` when unavailable. Independently inspected the change and ran its integration regression successfully.

## Independently checked fixes and contracts

- Coverage expands concrete address/topic alternatives and requires every partition to cover the complete accepted batch; short successful shards cannot borrow another partition's coverage.
- Pool roster preserves registered pools with no active events; discovery lifetime prevents pre-birth zero history.
- Native-unit baselines include quiet zeros and earlier priced observations in the same native unit. Fractional medians retain an exact numerator/denominator, and insufficient/zero baselines remain null.
- Invalid current prefixes remain unknown; multiplier rankings exclude a stale last-closed minute.
- Metadata known-height hash conflicts remove affected decimals from valuation. Missing-height evidence remains an explicitly documented historical carry-forward assumption, not current verification.
- Selected reports filter valuations, fees, and referenced quote evidence. CLI window summaries retain the named current/recent/natural windows, while full minute rows remain available in persisted reports.
- USDG valuation selects one side; indirect valuation uses preceding chain-position quotes and conservative age intervals. Cross-pool transaction counts are set unions; longer windows rebuild transaction sets.
- Observation annotations reject future-block and same-height conflicting-hash inputs. Liquidity and gross-fee descriptions avoid claims of verified withdrawals, current dollar liquidity, or LP net income.
- Fresh P2 snapshots are required and metric persistence is atomic; repairs recompute active-event results.

## Verification

Executed directly with the repository Node/Vitest runtime:

`node node_modules/vitest/vitest.mjs run tests/integration/metrics-repair.test.ts tests/unit/windows.test.ts`

Result: **2 test files passed, 17 tests passed**. This includes the newly added RWA invalid-prefix regression. No application source or test files were changed by this reviewer. No full-suite rerun or live-chain verification was performed.