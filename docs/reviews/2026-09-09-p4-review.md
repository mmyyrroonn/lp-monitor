# P4 whole-branch correctness review — 2026-09-09

**Scoped PASS. No unresolved actionable correctness findings in the reviewed final source.** Full-suite, build, artifact and acceptance-document gates remain owned by the parent task; this review does not substitute for those gates.

## Scope

Reviewed working branch `codex/p4-alerts` against `main` at `0c2e2e1`: every tracked source diff in `src/cli.ts`, `src/ops/recorder-cli.ts`, `src/ops/recorder.ts`, `src/storage/database.ts`, plus the final `src/metrics/windows.ts` and `src/storage/metric-store.ts` changes; every new `src/signals/*.ts` and `src/notify/*.ts`; migration `004-alerts.sql`; initial signal JSON; all new P4 tests and helpers including `alert-new-pool.test.ts`. Read the P4 plan, design section 10, task reports, prior engine/storage findings, final `.superpowers/sdd/p4-storage-fixes.md`, and relevant P1/P2/P3 call paths.

## Findings resolved during review

- **Registry recovery withdrawal delivery:** an offline probe demonstrated that startup discovery recovery could commit a withdrawal, then fail historical discovery before any drain: revision 2 remained pending with attempts 0. Recorder now drains only committed withdrawals immediately after registry recovery and retries prior withdrawals before new RPC. The permanent fork-plus-discovery-outage regression passes and excludes stale hot delivery.
- **Provenance-aware deduplication:** duplicate decisions now compare fully assembled proposed records including provenance, with normalized revision. The 100-repeat regression checks one revision/outbox row directly, independently of source-hash caching.
- **Async outbox supersession:** each cached row is rechecked before delivery; both success and failure status writes preserve superseded state. Regressions cover supersession of a later cached row and rejection of the currently in-flight row after supersession. Retraction-only delivery leaves ordinary pending records untouched.
- **Coverage repair:** incomplete historical baseline repair now invalidates affected reminders; ordinary current partial progression is exempt only while it remains valid partial or becomes complete. Current partial-to-gap regression is included. Retractions preserve original trigger metrics as audit evidence; repaired metrics are verified separately.
- **Standalone upgrade and output window identity:** reviewed the independent already-hot same-scale doubling fix, active-cooldown regression, separate recent/natural-five-minute baselines and counts, and current-minute counts for candidates without closed history.
- **Birth-minute candidate:** the narrowly scoped P3 adapter change exposes observed current-minute activity only when the prefix is valid and pool birth is already within scanned coverage. Closed birth minutes and prebirth history remain warming/null and excluded from baseline. The integration regression covers 25k candidate emission and later birth-minute baseline exclusion. Metric version advances to `p3-v3` to bind this semantic change.

## Confirmed contracts

Raw acceptance, P2 rebuild, metric/signal evaluation, snapshot/revision persistence and outbox enqueue share an outer SQLite transaction. Local file/console delivery occurs after commit. Source/config/version changes and recovery conservatively withdraw unsupported evidence and establish a new episode epoch. Historical/synthetic capture remains excluded from live delivery.

Candidate and natural-five-minute rules retain independent match outcomes, rational bigint comparisons, insufficient/zero-baseline behavior, cooldown, standalone same-scale doubling, cooling and reheat. Liquidity changes remain annotations as specified by design section 10; no automatic withdrawal inference or invented liquidity-watch rule was added. CLI notification requires explicit `follow --notify local`; ordinary recording, ingest and strict local-only signal configuration remain separated.

## Verification and limits

This reviewer independently ran only the focused synthetic discovery-outage probe, which reproduced the original defect. No public RPC was used. Its script is `.superpowers/sdd/p4-review-probe.ts`; original local evidence is under `C:/Users/myron/AppData/Local/Temp/p4-review-discovery-MkbN2n`.

Final source and permanent regressions were inspected after the fixes. Validation evidence read from the implementing agents: final storage/outbox/recorder suites 25/25 plus TypeScript, scoped formatting and diff checks; recorder/CLI regression set 19 passed; new-pool/windows/metrics-repair set 36 passed; formatter set 13 passed. Parent also independently reported final outbox/reorg 20/20. These are attributed results, not reruns by this reviewer. Earlier engine report records 20 passing signal/baseline tests.

No source edits or commits were made by this reviewer. No throughput, live latency, notification exactly-once, profitability or deployment claim is made. Full-scope rebuild performance and provider coverage remain explicit practical limits, and local crash retries remain identifiable by alert id/revision.
