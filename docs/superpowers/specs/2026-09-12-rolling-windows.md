# 四种实时滚动窗口

用户要求移除自然时间窗口，仅保留过去1m/5m并新增15m/1h。统一以已接受链上时间T为锚，区间(T-duration,T]，左边界过期、右边界纳入。池与RWA输出一致。

分钟汇总继续作为内部存储/历史回放材料。实时不构建自然五分钟桶；滚动边界使用事件时间，无法判定则保留unknown。对同一交易去重，缺失估值不补零，完整无成交为零。新池不足窗口为warming。

1m/5m基线与信号同步迁移，连续确认及降温使用不重叠区间；新增15m/1h只作统计。历史补评保持backfill隔离，重组保留撤回机制。CLI、排名及通知同步更新。显式minute-close离线回放保留兼容。

验证：非整分钟终点、边界过期、空窗口/缺口/未知估值、重复轮询、修订/撤回、CLI四窗口、全套离线测试及类型/格式/构建。未运行实链采集。

验证结果：2026-09-12，81 个测试文件、792 项测试通过；typecheck、lint、build 与 git diff --check 通过。未运行实链采集；改动未提交。
