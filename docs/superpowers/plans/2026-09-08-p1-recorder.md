# P1 范围日志、分钟时间与恢复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完整记录观察范围的logs，以少量端点和分钟边界查询支持热度统计及修正。

**Architecture:** 默认每2秒取连续范围；取消每块Header/parentHash账本。原始数据与有效日志集合分开，分钟归桶无需逐块时间。

**Tech Stack:** Node.js 24、TypeScript strict ESM、pnpm、viem、SQLite；协议格式优先官方 ABI/SDK。

## Global Constraints

- 只读链数据；生产热路径不依赖 Uni API、Subgraph 服务或 GMGN。
- Node.js 24、TypeScript strict ESM、pnpm；精确版本与 lockfile 由 P0 固化。
- 原始金额 bigint；持久化用无损字符串；业务结果绑定 chainId、blockHash 与版本。
- 实时与历史共用业务核心；缺口不补零，未知估值/费用不写零。
- 同块动态发现、断点恢复、重组与告警撤回必须有测试。
- 秘钥仅从本机环境读取；日志保存 provider alias，不保存带凭据 URL。
- 现有 research/、tooling/、.agents/、.pnpm-store/ 保留，不纳入应用依赖重构。
- 每窗口只实现指定阶段；更新状态和证据，未验收不得勾成完成。
- 默认只提供 RPC、本机增量同步；无付费索引后台依赖，记录调用用量与预算。

所有相对代码路径以 E:/lp-monitor 为根。架构依据：[设计](../specs/2026-09-08-robinhood-rwa-monitor-design.md)。下列代码块定义接口、关键算法和测试输入；实际实现需围绕这些合同补齐本阶段列出的行为，不是把文档片段原样拼成生产程序。命令均为未来阶段实现后的验收入口，本次写计划没有运行它们。

## Task 1.1：范围账本与有效集合

**Files:** 创建 src/storage/database.ts、src/storage/migrations/001-raw.sql、src/storage/raw-store.ts、src/storage/manifest.ts、src/ingest/completeness.ts、tests/integration/raw-store.test.ts。

**Interfaces:** 实现设计RangeStore、RangeBatch、RangeChangeSet；acceptRange只接受所有分片成功且过滤范围明确的输入，并原子返回added/removed/retimed集合。

- [ ] cursor和active范围按scopeId隔离；acceptedTip(scopeId)、invalidateAfter(scopeId,anchor)显式带scope。scope由观察集合/协议/事件族确定，不因新池分片或本次manifest改变。新观察scope回填后启用，禁止沿用旧scope的完成水位。

- [ ] 建ingest_batches、fetch_shards、raw_logs、accepted_ranges、active_logs、anchors、minute_boundaries、log_times、runs、checkpoints；不建每块Header或canonical_blocks表。
- [ ] 原始日志按chainId/blockHash/txHash/logIndex幂等保存；raw响应不可变，旧分支日志仍保留。数量bigint序列化，SQLite高度范围检查。
- [ ] 先测试以下集合对账；批次写入、有效集合和cursor提交为事务，DB写满/中断不得假推进。

~~~text
同一filter F、范围101–120：旧有效集合{A,B}，新完整集合{B,C}
结果 active={B,C}，raw仍有{A,B,C}；removed={A}，added={C}
新结果为空且所有F分片完整 => 原有范围内日志退出active
任一分片失败/疑似截断 => 不改变active和完成cursor
改用filter G => 不能将F范围内的A误删
相同完整批次再读一次 => added/removed均空
~~~

- [ ] 保存无密钥请求参数、响应hash、期望/已完成分片、过滤版本；HTTP200不自动代表未截断。
- [ ] 运行 pnpm exec vitest run tests/integration/raw-store.test.ts。

## Task 1.2：两遍动态发现与连续范围

**Files:** 创建 src/registry/assets.ts、src/registry/pools.ts、src/protocols/uniswap-v3/discover.ts、src/protocols/uniswap-v4/discover.ts、src/ingest/filter-plan.ts、src/ingest/fetch-range.ts、tests/integration/discovery.test.ts。

**Interfaces:** PoolRegistration含pool、token0/1、feePips、tickSpacing、hooks、discoveredAt、assetVersion、source；discoverPools(logs,assetVersion)返回登记；fetchRange(from,to,end,registry)返回RangeBatch。

- [ ] 使用官方ABI。先发现V3 PoolCreated/V4 Initialize，两侧RWA取并集，再抓本段扩大后的全部观察池操作；两侧都被观察不得重复计数。
- [ ] 同块fixture：block100 log2建池P、log3 Initialize、log4 Mint、log5 Swap；第二遍必须覆盖P的整个block100，不能从101开始。相同代币不同V4 fee/hooks保持不同poolId。
- [ ] 参考lp-terminal adaptiveLogs/v4Rpc的范围扫描，不继承logtail跳过积压行为；新增资产先补其发现历史，冷池不删除。
- [ ] 地址/topic分片、范围二分与单块超限继续分片；429按预算退避。两遍暂存池登记直到范围成功，分叉恢复可使错误分支登记失效。
- [ ] 初始logResponseGuard=5000，结合P0的maxLogsPerResponse|null，达到已知较小上限或有截断提示即拆分。未知上限保留unknown；用受限fixture模拟5000条封顶、单块超限、部分分片失败，不以HTTP200直接删除旧集合。
- [ ] 运行 pnpm exec vitest run tests/integration/discovery.test.ts。

## Task 1.3：分钟时间索引

**Files:** 创建 src/ingest/time-index.ts、src/ingest/log-time.ts、tests/unit/time-index.test.ts、tests/integration/log-time.test.ts。

**Interfaces:** resolveMinuteBoundary(reader,T,lo,hi): Promise<MinuteBoundary>；assignLogMinutes(logs,anchors,boundaries): ReadonlyMap<string,LogTime>。前置lo.timestampSec<T<=hi.timestampSec；不满足时先扩展已有稀疏anchors或报告unresolved。

- [ ] 将docs/research/evidence/2026-09-08-log-timestamps.json中的0x0时间作为真实失败fixture；字段存在不等于时间可用。不调用RPC修复该固定fixture。
- [ ] 实现lower_bound，缓存并保存实际anchor响应；定位B后验证B-1与B。伪代码定义如下，不按估计块速插值。

~~~text
while hi.number - lo.number > 1:
  mid = getAnchor((lo.number + hi.number) / 2)
  mid.time >= T ? hi = mid : lo = mid
返回 firstBlock=hi.number, before=lo, at=hi
要求 lo.time < T <= hi.time
~~~

- [ ] 测试块时间59,59,60,60,61，在T=60时选第一个60；59归前分钟，两个60都归后分钟。同分钟的一批不逐块查询。分钟索引不证明具体事件秒数，exactTimestampSec保持null。
- [ ] 测试历史回填到达时间在今天，成交时间仍按历史分钟；边界查询失败只让受影响时间窗unresolved，原始logs照常落盘。时间逆序/冲突不得填零或插值。
- [ ] 测试cursor=120位于新分钟、重扫101–120包含旧分钟日志：不能用cursor/end时间给整个overlap归桶。新出现的旧日志使用包围其高度的anchors；补齐/修正时间返回retimed并重算旧桶，不能只处理added日志。
- [ ] 正常20块跨度中，验证getAnchor调用与批端点/跨分钟边界有关；不出现遍历每个块读取时间的循环。运行 pnpm exec vitest run tests/unit/time-index.test.ts tests/integration/log-time.test.ts。

## Task 1.4：跟随、重扫与恢复

**Files:** 创建 src/ingest/follow.ts、src/ingest/reorg.ts、src/ingest/checkpoint.ts、src/ops/health.ts、tests/integration/reorg.test.ts、tests/integration/resume.test.ts；修改src/cli.ts。

**Interfaces:** follow(reader,store,stopAtMs): Promise<void>；findMatchingCheckpoint(reader,checkpoints): Promise<BlockAnchor|null>。invalidateAfter后补扫，再修订有效集合与分钟索引。

- [ ] 默认pollIntervalMs=2000、maxRangeBlocks=1000、overlapBlocks=20；先固定目标end anchor，再验证上一完成cursor的hash。正常链头仅少量端点读取；积压分段有独立end anchor。同一cursor单任务，不在补段之间再等待2秒。
- [ ] 按max(start,cursor-overlapBlocks+1)重扫近期部分并覆盖新增区间；无前次cursor时从显式start开始。旧日志沿用已保存归桶证据，不能因重扫而挪到当前分钟。
- [ ] 测试cursor100/head127一次扫描全部新增101–127；有已存历史时含近期重扫。head2127连续分段；空链头不推进；分片失败后恢复不漏事件。
- [ ] 测试checkpoint A100/A120，A120 hash变成B120时查已有检查点并恢复A100；删除受影响active、分钟索引和错误池发现，再接新分支。超过保留点则warmup重建并标旧段待核验。
- [ ] 配置warmupMinutes=60、checkpointRetentionMinutes=180；找匹配点用指数扩展加二分，最多32次anchor调用并受统一限流，避免逐个查数千检查点。边界before或at证据失效时清除相关归桶缓存。
- [ ] 每成功范围保存end检查点；warmup起点由head链时间减60分钟的分钟边界决定且不早于协议部署。测试分叉后同一tx移到新分钟（blockHash变化），旧桶扣除、新桶增加；再测active日志由unresolved补成minute时retimed非空、added为空，仍修订聚合和提醒。
- [ ] 测试旧日志消失时受影响窗口与提醒能被重建，不能只插入新日志；断线/重启/切RPC先验证cursor。固定20块窗口不命名最终性。
- [ ] 实现 pnpm lp ingest --config config/robinhood.json --from-block 100 --to-block 200 与 pnpm lp follow --config config/robinhood.json --duration 10m；高度仅CLI示例。
- [ ] 运行P1所有上述测试、typecheck/build；有界真实录制并重启，保存manifest与端点/分钟边界方法数；更新状态后停止。

## 验收

同块新池无漏、重试幂等、过滤覆盖隔离、消失日志从有效集合退出、分钟边界正确、断线恢复；正常采集不逐块取Header。保留RPC一致性假设与provisional状态，不能宣称全链独立校验。下窗口P2。
