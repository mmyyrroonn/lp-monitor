# P5 历史回放与“什么叫火热” Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用分钟历史样本比较热度阈值、提醒后成交量和持续时间，保留很快冷却的候选。

**Architecture:** recorded-live/backfill输入相同pipeline，回放关闭网络并注入时钟；命名案例用于回归，完整出生队列用于评估，二者分列。

**Tech Stack:** Node.js 24、TypeScript strict ESM、pnpm、viem、SQLite；协议格式优先官方 ABI/SDK。


## 2026-09-10 恢复实施与验收口径

用户已明确要求实现 P5；Task 5.1–5.3 恢复执行。沿用“只使用已有历史、不新增采集或专门回填”的约束。代码验收与完整历史效果评估分别记录：实现完成后运行所有检查，已有历史不足时，历史结论仍为 incomplete。

实现细化：grid 的相对倍数用于 1m 候选，1/2 根轴用于自然 5m 绝对量确认，研究配置关闭并行 confirmRelative，以免单根相对确认绕过两根要求。现有默认实时信号配置保持原值。探索结果窗在 09-06 起点右截尾，验证结果窗在 09-08 起点右截尾。命名案例及完整出生队列不能合并为独立样本分母。

## 历史优先级记录 — 2026-09-09用户调整（现按上方恢复实施）

此前P5保持pending、低优先级。历史获取已有能力和数据保留，缺少的暂时不补；不为本计划专门新增采集功能或补齐研究历史。P5完整历史评估不再作为P6实时运行验证的前置条件。

该次调整曾暂缓固定案例、完整出生队列和参数网格；2026-09-10用户要求恢复实施，使用已有历史与积累的实时录制数据，缺数据明确报告，不填零或声称评估完成。实时启动、池发现、恢复和数据完整性必需的既有扫描不受此调整影响。

用户可通过额外分析提供阈值，记录适用对象、指标单位、时间窗口、条件关系与分析来源；经口径核对后作为候选配置。阈值来源与独立验证结果分开，未验证效果保持未知。本次不修改signals配置。

## Global Constraints

- 只读链数据；生产热路径不依赖 Uni API、Subgraph 服务或 GMGN。
- Node.js 24、TypeScript strict ESM、pnpm；精确版本与 lockfile 由 P0 固化。
- 原始金额 bigint；持久化用无损字符串；业务结果绑定 chainId、blockHash 与版本。
- 实时与历史共用业务核心；缺口不补零，未知估值/费用不写零。
- 同块动态发现、断点恢复、重组与告警撤回必须有测试。
- 秘钥仅从本机环境读取；日志保存 provider alias，不保存带凭据 URL。
- 现有 research/、tooling/、.agents/、.pnpm-store/ 保留，不纳入应用依赖重构。
- 每窗口只实现指定阶段；更新状态和证据，未验收不得勾成完成。
- 默认只提供 RPC、本机增量同步；无付费索引后台依赖，记录调用用量与预算。

所有相对代码路径以 E:/lp-monitor 为根。架构依据：[设计](../specs/2026-09-08-robinhood-rwa-monitor-design.md)。下列代码块定义接口、关键算法和测试输入；实际实现需围绕这些合同补齐本阶段列出的行为，不是把文档片段原样拼成生产程序。命令均为未来阶段实现后的验收入口，本次写计划没有运行它们。

## Task 5.1：离线驱动与确定性

**Files:** 创建 src/replay/reader.ts、src/replay/clock.ts、src/replay/runner.ts、src/replay/integrity.ts、tests/integration/replay-equivalence.test.ts、tests/integration/replay-quality.test.ts；修改src/cli.ts。

**Interfaces:** replay(manifest, config, mode): Promise<ReplayReport>；mode为minute-close或recorded-observed。保存raw/分钟索引/配置/ABI/code hash。实录按原始批次复算，历史按完整分钟评估。

- [x] 用下面顺序验证相同语义；测试禁止网络请求，quote/metadata/state读取只允许来自manifest：

~~~text
同一段recorded输入保留原批次边界与observedAt:
  live投影与决策
  replay，按原批次输入，并从导出的可移植manifest再次回放
reader整份读取JSON；不声称分块或流式读取验收。
业务内容hash一致；改变轮询批次可以改变告警，不能要求强行相同。
删除一份范围分片或必要分钟边界证据 => 相关历史标不完整。
替换配置版本 => 新replayRun，不覆盖旧实验。
~~~

- [x] 历史minute-close将整分钟数据在分钟结束后交给规则；实时本分钟候选的最早秒数不能从分钟桶反推。recorded-observed保留真实到达时间；回填到达时间不作过去成交时间。
- [x] 资产名/身份与规则使用当时可用版本；今天的RWA登记表作为历史队列过滤条件时必须标retrospective-cohort，不冒充当时已知watchlist。
- [ ] 原生 P1 直接回放验收：pnpm lp replay --manifest artifacts/p1/manifest.json --rules config/signals.initial.json --mode minute-close --out artifacts/p5/replay。原生 manifest 未保存 replay.input，当前只能返回 incomplete / 0 帧；从库和运行摘要导出历史输入快照的入口尚未实现。
- [x] 带 replay.input 和完整产物证据的可移植 manifest 已提供 replay CLI；合成样本可回放及再次回放。
- [x] 运行 pnpm exec vitest run tests/integration/replay-equivalence.test.ts tests/integration/replay-quality.test.ts。

## Task 5.2：固定案例与完整队列

**Files:** 创建 config/history.cases.json、src/replay/cohort.ts、src/replay/outcomes.ts、src/replay/quality-report.ts、tests/unit/outcomes.test.ts、tests/integration/cohort.test.ts。

**Interfaces:** buildCohort(discoveryRange, registryVersion): Cohort；evaluateOutcomes(alert, futureEvents, coverage): Outcome。Outcome包含随后15/60/180m目标池量、Swap/tx次数、持续活跃分钟、覆盖及右截尾；毛费附注可选，无仓位回报模拟。

- [ ] 注册首批UTC观察区间；在捕获时扩展基线warmup与3h结果窗，不把以下核心区间当完整采样范围：

~~~text
AMC Meme 首发：2026-09-04T03:25:00Z 至 2026-09-04T09:25:00Z
MEME 早期及再热：2026-09-03T20:30:00Z 至 2026-09-04T12:00:00Z
MEME 大幅再热：2026-09-06T17:00:00Z 至 2026-09-06T23:00:00Z
观察资产：AMC；目标费用池：AMC/USDG V3 与 V4；关联池含MEME/AMC和AMC Meme/AMC
~~~

- [x] 使用P1分钟边界索引和范围logs；只取实际必要的稀疏anchors，无逐块headers或archive state要求。历史端点缺日志/边界证据则对应结果不完整。
- [x] 对研究里的cyberbeer/弱样本，从保存的token身份与创建证据推得窗口，不依赖当前排行榜；未知地址先核验，symbol不能匹配成另一币。
- [ ] 完整出生队列起始候选为2026-09-03T00:00:00Z至2026-09-07T00:00:00Z内AMC相关Uni新池，后续保留足够3h结局；再热评估另包含区间前已存在池，不能只选新池。
- [x] 测试一个“创建后无成交死亡”池仍在分母；一个结束前不足3h的池标censored；一个历史缺块池标incomplete；不能当0或删除。
- [x] minute-close结果从触发分钟之后开始统计，排除整个触发分钟。Meme池与RWA/USDG池分列；分钟粒度结果不能写成逐笔秒级发现时间。实录若有更细证据另列。
- [x] 运行 pnpm exec vitest run tests/unit/outcomes.test.ts tests/integration/cohort.test.ts。

## Task 5.3：阈值比较与结论

**Files:** 创建 config/signals.grid.json、src/replay/experiments.ts、src/replay/report.ts、tests/unit/experiment-split.test.ts、artifacts/p5/report.md、artifacts/p5/results.json。

- [x] 固定候选grid：1m绝对量10k/20k/50k；相对倍数3/5/10；5m确认50k/100k；1根/2根确认；300s同级冷却。记录每组参数，限制组合数并报告全部结果，不只选冠军。
- [x] 以时间切分：09-03至09-05作探索，09-06至09-07作验证；同币跨期依赖单列，补leave-one-token-out敏感性。样本小就报告区间与数量，不包装可靠胜率。
- [x] 对比规则的报警数、独立机会episode数、报警提前/延迟、后续15/60/180m目标池量、持续时间、收费估计可信度、每小时通知数。
- [ ] 量级1M/500k事后episode描述标签尚未实现；这些未来量级不能作为实时输入的限制已保持。
- [x] minute-close实验比较额外0/1/5分钟反应延迟；更细秒级延迟仅在实录证据支持时评估。delay=0 包含发报当分钟，可能计入发报前最多59秒，仅作敏感性对照；delay=1/5 为主口径。分钟实验不证明当前分钟候选的最早时点。
- [x] 解释GMGN与链上差异但不强行对齐数字。现有AMC研究的1.83%/4.57%仅作为待核验展示强度，报告不复述成监控策略收益。
- [x] 执行 pnpm lp study --cases config/history.cases.json --grid config/signals.grid.json --out artifacts/p5；该命令默认使用已有raw，不自动大额回填。
- [x] typecheck/build/P5测试通过，报告coverage和策略结论分开；若没有足够历史，只能通过代码验收、历史阶段仍incomplete。更新状态后停止。

## 验收

完全离线复算分钟指标；相同实录批次得到相同结果；无整分钟未来量提前进入规则；保留死亡/冷池/缺口/右截尾；结果评价热度持续而非LP净收益。是否发现有效规则由数据决定，允许结论是“现有阈值尚无足够证据”。


## 2026-09-10 独立 Review 后交接

此前623项测试通过不足以证明P5通过；本轮正确性修复通过全套649项测试、typecheck/lint/build和构建后CLI检查，未完成能力仍保留，详见 [验收](../../reviews/2026-09-10-p5-acceptance.md)。历史评估仍 incomplete：七个案例不可用、四份原生P1无评估帧、36组候选未产生告警/结局窗口/首告警比较记录。非空路径只由合成fixture覆盖。

原生P1导出入口、1M/500k描述标签、完整真实采样与长窗口性能仍未完成。minute-close每分钟全量投影与计价，不能宣称四天完整队列已可实用运行；可复现实测见 [性能证据](../../../artifacts/p5/benchmark.json)。

P5按本轮Review范围修复；验收时尚未提交，后续提交合并状态以Git记录为准。未启动P6或新增历史采集。研究报告见 [report.md](../../../artifacts/p5/report.md)。操作范围重叠修订顺序不明及历史快照缺失均保守incomplete；当前分钟最早秒数不由分钟桶反推。
