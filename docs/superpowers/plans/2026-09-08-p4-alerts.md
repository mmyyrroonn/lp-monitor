# P4 短窗机会检测与本机提醒 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 及时发出有证据的候选、热度确认、再热和流动性变化提醒。

**Architecture:** 纯规则引擎消费带覆盖状态的metrics；告警经事务outbox输出，实时和回放走同一决策函数。

**Tech Stack:** Node.js 24、TypeScript strict ESM、pnpm、viem、SQLite；协议格式优先官方 ABI/SDK。

## Global Constraints

- 只读链数据；生产热路径不依赖 Uni API、Subgraph 服务或 GMGN。
- Node.js 24、TypeScript strict ESM、pnpm；精确版本与 lockfile 由 P0 固化。
- 原始金额 bigint；持久化用无损字符串；业务结果绑定 chainId、blockHash 与版本。
- 实时与历史共用业务核心；缺口不补零，未知估值/费用不写零。
- 同块动态发现、断点恢复、重组与告警撤回必须有测试。
- 秘钥仅从本机环境读取；日志保存 provider alias，不保存带凭据 URL。
- 现有 research/、tooling/、.agents/、.pnpm-store/ 保留，不纳入应用依赖重构。
- 每窗口只实现指定阶段；更新状态和证据，未验收不得勾成完成。
- 默认只提供 RPC、本机增量同步；无付费索引后台依赖，记录调用用量与预算。

所有相对代码路径以 E:/lp-monitor 为根。架构依据：[设计](../specs/2026-09-08-robinhood-rwa-monitor-design.md)。下列代码块定义接口、关键算法和测试输入；实际实现需围绕这些合同补齐本阶段列出的行为，不是把文档片段原样拼成生产程序。命令均为未来阶段实现后的验收入口，本次写计划没有运行它们。

## Task 4.1：规则配置与状态机

**Files:** 创建 config/signals.initial.json、src/signals/config.ts、src/signals/engine.ts、src/signals/baseline.ts、src/signals/types.ts、tests/unit/signals.test.ts、tests/unit/signals-baseline.test.ts。

**Interfaces:** evaluateSignal(previous: SignalSnapshot, input: SignalInput, config: SignalConfig): SignalDecision；SignalInput含pool、batchId、endAnchor、watermarkSec、metrics、coverage；SignalDecision含nextSnapshot与可选AlertDraft，不直接发通知。

- [x] 实现设计第10节的起始参数：本分钟累计>=20k且相对前60个完整1m桶5倍；5m>=100k且前12桶5倍；另一候选规则连续两桶>=50k；同级冷却300s；翻倍升级；连续3桶低于进入阈值25%降温。
- [x] 配置中每条规则有enabled/threshold/version；两条确认规则分别输出命中原因，P5逐条比较，不能只保留OR合并后的结果。
- [x] 先写有预期输出的状态序列：

~~~text
新池，无基线，本分钟累计=25k => candidate + warming/absolute-only
基线5m=10k，当前完整5m=120k => hot
2分钟后同级100k => 不重复提醒
冷却期间新窗口300k => 升级提醒，不被300秒屏蔽
三根完整桶都低于进入阈值25% => cooling
cooling后再次120k且满足基线 => hot + reheat
数据中断15分钟 => 不作降温，不填0，不移除池
~~~

- [x] baseline只读当前窗口之前完整同尺度桶；不足时禁用相对倍数，保留绝对量候选。中位数为0标zero-baseline，不能显示Infinity。
- [x] 加减池动作和最近Swap的L观测仅作附注；如输出liquidity-watch，不能只凭L下降断言撤资。实时本分钟候选每批可更新，标partial，不承诺精确滚动60s。
- [x] 运行 pnpm exec vitest run tests/unit/signals.test.ts tests/unit/signals-baseline.test.ts；确保再热不依赖市值已达1M。

## Task 4.2：outbox、重复控制与撤回

**Files:** 创建 src/storage/migrations/004-alerts.sql、src/signals/project.ts、src/notify/outbox.ts、src/notify/console.ts、src/notify/jsonl.ts、tests/integration/alert-outbox.test.ts、tests/integration/alert-reorg.test.ts。

**Interfaces:** commitSignalDecision(batch, decision): void（在范围有效集合及投影事务内）；deliverPending(sink): Promise<DeliverySummary>。id由chain/pool/ruleVersion/episode/kind构成稳定hash，记录atBatchId与实际observedAtMs；修订加revision。

- [x] 测试相同输入重放100次只生成同一AlertDraft；事务提交前崩溃不投递；提交后/投递前崩溃可继续。文件写入后/标记sent前崩溃允许重复delivery尝试，以id/revision识别，不能承诺外部严格exactly-once。
- [x] outbox存pending/sent/failed及attempt次数；JSONL消费者按id/revision去重；本机控制台注明revision。普通backfill/replay不进入实时通知sink。
- [x] 分叉前发hot、分叉后条件消失，原提醒生成retracted修订；若新分支再次达标，产生绑定新blockHash的提醒。空窗恢复不能漏发修订。
- [x] 首版提醒统一provisional；重扫修正可以更正/撤回，不做固定确认深度后的finalized升级。
- [x] 运行 pnpm exec vitest run tests/integration/alert-outbox.test.ts tests/integration/alert-reorg.test.ts。

## Task 4.3：人可读输出与边界

**Files:** 创建 src/notify/format.ts、tests/unit/alert-format.test.ts；修改src/cli.ts。

- [x] 文本包含RWA、pool、关联代币、到达时间、扫描高度、本分钟累计/完整5m量、基线倍数、Swap/不同tx次数、流动性动作与minute精度；未知值不显示0。
- [x] 使用下面的信息布局；数字只是格式示例，运行时必须来自真实metrics：

~~~text
AMC / USDG — 热度确认（暂未最终确认）
最近5m：120,000 USDG；过去基线：10,000；12倍
Swap：200次；不同tx：180笔；加池动作5次、减池动作2次
时间：分钟归桶；本分钟累计仍在更新；数据可能修订
关联Meme：MEME（同交易共现，尚未证实完整路由）
数据：完整至区块…；证据：tx…；告警id/revision…
~~~

- [x] follow 增加 --notify local；不指定则默认只记录。config禁止自动使用聊天/email/webhook地址，用户确定渠道后才另接sink。
- [x] typecheck/build/P4测试通过后，用离线fixture展示全部提醒类型，保存artifacts/p4/alerts.jsonl与rendered.txt；状态更新后停止P4。

## 验收

每批完整范围可更新本分钟候选；5m确认不改变2秒扫描节奏；冷却不屏蔽升级/再热；断线不当降温；日志集合修正会更正提醒；输出有分钟精度、池与证据。

## P4实施验收 — 2026-09-09

已完成Task4.1–4.3。539测试通过；lint/typecheck/build/离线验收/diff检查均退出0；[独立审查PASS](../../reviews/2026-09-09-p4-review.md)、[验收与边界](../../reviews/2026-09-09-p4-acceptance.md)。基线测试实际文件命名为signals-baseline.test.ts以与P3模块区分。新池首分钟partial适配令metric version升至p3-v3；出生前/不完整出生分钟不进入基线。liquidity-watch只有格式示例，加减动作/L仅作附注。P5/P6未开始；未实链运行、提交或推送。